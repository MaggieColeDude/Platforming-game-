import pygame, sys
from button import Button 

pygame.init()

SCREEN = pygame.display.set_mode((1280, 720))
pygame.display.set_caption("Menu")

BG = pygame.image.load("assets/Background.png")

def get_font(size): #retuns start to size
  
def play():

def options(): 

def main_menu(): #Menu Screen 
  pygame.display.set_caption("Menu")
  
  while True:
    SCREEN.blit(BG, (0, 0))
    
    MENU_MOUSE_POS = pygame.mouse.get_pos()
    
    MENU_TEXT = get_font(100).render("MAIN MENU", True, "#b68f40")
    MENU_RECT = MENU.TEXT.get_rect(center=(64,100))
    
    PLAY_BUTTON = Button(image=pygame.image.load("assets/Play Rect.png"), pos
                         text_input="PLAY", font=get_font(75), base_color="#d7fcd4", hovering_color= "White")
    OPTION_BUTTON = Button 
main_menu()

#Notes: How to use multiple screens in pygame. To use multiple screens in pygame, use multiple functions in thier own loops. 
